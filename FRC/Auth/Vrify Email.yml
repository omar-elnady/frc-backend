info:
  name: Vrify Email
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/auth/email-verification/verify"
  body:
    type: json
    data: |-
      {
        "email": "omae.elnadii@gmail.com",
        "activationCode": "4632"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
